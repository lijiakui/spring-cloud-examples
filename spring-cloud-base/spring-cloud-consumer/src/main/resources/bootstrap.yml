spring:
  cloud:
    config:
      name: hello-config
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: spring-cloud-config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/